{
  "name": "full-stack-app",
  "version": "1.0.0",
  "dependencies": {
    "bootstrap": "^5.3.2",
    "jwt-decode": "^4.0.0"
  },
  "scripts": {
    "start": "node backend/app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  }
}
