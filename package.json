{
  "name": "full-stack-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node my-website/server/app.js",
    "heroku-postbuild": "cd my-website/client && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  }
}
